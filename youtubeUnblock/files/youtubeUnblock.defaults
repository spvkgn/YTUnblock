#!/bin/sh

f=firewall.youtubeUnblock
uci get "$f" >/dev/null && exit 0
uci -q batch <<-EOF
	set $f=include
	set $f.type=script
	set $f.path=/etc/firewall.youtubeUnblock
	set $f.family=IPv4
	set $f.reload=1
	commit firewall
EOF

exit 0
